<div id="alerts-container">
  <% flash.each do |key, value| %>
    <div class="alert alert-info"><%= value %></div>
  <% end %>
</div>

<script>
  hideAlerts = function() {
    $("#alerts-container").hide();
  }

  setTimeout(hideAlerts, 2500);
</script>
